name: tier3b-fixture-20
on:
  pull_request:
    branches: [main]
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          doc-lattice check --maxfail=1 --disable-warnings --cache-clear || true
