# Hermetic clean lattice for the CI release smoke test. The doc set below carries
# no derives_from edges, so check and lint always exit 0 here regardless of the
# repository's own docs. Pointing the release smoke at this fixture keeps a future
# real docs/ lattice from reddening every release.
docs_roots:
  - docs
